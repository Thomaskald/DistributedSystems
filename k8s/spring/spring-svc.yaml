apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring
  name: spring
spec:
  selector:
    app: spring
  ports:
    - port: 8082
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
  type: NodePort
